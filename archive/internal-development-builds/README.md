# Internal development builds

These folders contain private installers created while Recall Flashcards was being developed.

They are retained for reference only and are not part of the public release history. The first public version is **v1.0.0** in the project-root `release-1.0.0` folder.

The previous detailed development changelog is saved as `DEVELOPMENT-CHANGELOG.md`.
